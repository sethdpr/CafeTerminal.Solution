<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="CafeTerminal.Maui.Views.MainPage"
    Title="Cafe Terminal">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="24">

            <!-- PRODUCTS -->
            <Label Text="Products" FontSize="22" FontAttributes="Bold" />

            <Entry
                Placeholder="Product name"
                Text="{Binding NewProductName}" />

            <Entry
                Placeholder="Price"
                Keyboard="Numeric"
                Text="{Binding NewProductPrice}" />

            <Button
                Text="Add product"
                Command="{Binding AddProductCommand}" />

            <CollectionView ItemsSource="{Binding Products}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <HorizontalStackLayout Spacing="12">
                            <Label Text="{Binding Name}" />
                            <Label Text="{Binding Price}" />
                            <Button
                                Text="Delete"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}},
                                                  Path=BindingContext.DeleteProductCommand}"
                                CommandParameter="{Binding .}" />
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- TABLES -->
            <Label Text="Tables" FontSize="22" FontAttributes="Bold" />

            <Entry
                Placeholder="Table name"
                Text="{Binding NewTableName}" />

            <Entry
                Placeholder="Table number"
                Keyboard="Numeric"
                Text="{Binding NewTableNumber}" />

            <Button
                Text="Add table"
                Command="{Binding AddTableCommand}" />

            <CollectionView ItemsSource="{Binding Tables}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <HorizontalStackLayout Spacing="12">
                            <Label Text="{Binding Name}" />
                            <Label Text="{Binding Number}" />
                            <Button
                                Text="Delete"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}},
                                                  Path=BindingContext.DeleteTableCommand}"
                                CommandParameter="{Binding .}" />
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- ORDERS -->
            <Label Text="Orders" FontSize="22" FontAttributes="Bold" />

            <Picker
                Title="Select table"
                ItemsSource="{Binding Tables}"
                ItemDisplayBinding="{Binding Name}"
                SelectedItem="{Binding SelectedTable}" />

            <Button
                Text="Add order"
                Command="{Binding AddOrderCommand}" />

            <CollectionView ItemsSource="{Binding Orders}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <VerticalStackLayout Spacing="4">
                            <Label Text="{Binding TableId, StringFormat='Table ID: {0}'}" />
                            <Label Text="{Binding CreatedAt, StringFormat='Created: {0}'}" />
                            <Label Text="{Binding IsClosed, StringFormat='Closed: {0}'}" />
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>